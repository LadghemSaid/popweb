twig:
    form_theme: ['bootstrap_4_layout.html.twig']

    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        app.path.job_images: '%env(string:UPLOAD_PATH_JOBS)%'
        app.path.project_images: '%env(string:UPLOAD_PATH_PROJECTS)%'

