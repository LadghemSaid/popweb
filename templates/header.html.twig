<header>

    {# <div class="nav">
         <input type="checkbox" id="nav-check">
         <div class="nav-header">
             <div class="nav-title">
                 <a href="{{ path("index") }}"><img src="{{ asset("img/logo.svg") }}" alt="Agence Pop Web logo"/></a>
             </div>
         </div>
         <div class="nav-btn">
             <label for="nav-check">
                 <span></span>
                 <span></span>
                 <span></span>
             </label>
         </div>

         <div class="nav-links">
             <a href="//github.io/jo_geek" target="_blank">Github</a>
             <a href="http://stackoverflow.com/users/4084003/" target="_blank">Stackoverflow</a>
             <a href="https://in.linkedin.com/in/jonesvinothjoseph" target="_blank">LinkedIn</a>
             <a href="https://codepen.io/jo_Geek/" target="_blank">Codepen</a>
             <a href="https://jsfiddle.net/user/jo_Geek/" target="_blank">JsFiddle</a>
         </div>
     </div>#}


        <nav class="navbar navbar-expand-lg">
            <a class="navbar-brand" href="#"><img src="{{ asset("img/logo.svg") }}" alt="Agence Pop Web logo"/></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse nav_right" id="navbarSupportedContent">
                <ul class="navbar-nav">
                    <li>
                        <a class="nav-link {% if current_menu is defined and current_menu == 'about' %}active{% endif %}"
                           href="{{ path('about') }}">Agence</a></li>
                    <li>
                        <a class="nav-link {% if current_menu is defined and current_menu == 'service' %}active{% endif %}"
                           href="{{ path('service.index') }}">Services <span class="sr-only">(current)</span></a></li>
                    <li>
                        <a class="nav-link {% if current_menu is defined and current_menu == 'projects' %}active{% endif %}"
                           href="{{ path('project.index') }}">Réalisations <span class="sr-only">(current)</span></a>
                    </li>
                    <li>
                        <a class="nav-link {% if current_menu is defined and current_menu == 'articles' %}active{% endif %}"
                           href="{{ path('articles.index') }}">Blog <span class="sr-only">(current)</span></a></li>
                    <li><a class="nav-link {% if current_menu is defined and current_menu == 'jobs' %}active{% endif %}"
                           href="{{ path('job.index') }}">Recrutement</a></li>
                    <li>
                        <a class="nav-link {% if current_menu is defined and current_menu == 'contact' %}active{% endif %}"
                           href="{{ path('contact.show') }}">Contactez-nous</a></li>
                    {% if app.user %}


                        {% if app.environment == "dev" %}
                            <li><a class="nav-link " href="/admin">Admin</a></li>
                        {% endif %}

                        {% if app.environment == "prod" %}
                            <li><a class="nav-link " href="/admin">Admin</a></li>
                        {% endif %}
                        <li><a class=" {% if current_menu is defined and current_menu == 'logout' %}active{% endif %}"
                               href="{{ path('app_logout') }}">Se déconnecter</a></li>

                    {% else %}
{#                        <a class=" {% if current_menu is defined and current_menu == 'login' %}active{% endif %}"#}
{#                           href="{{ path('app_login') }}">Se connecter</a>#}
{#                        <a class=" {% if current_menu is defined and current_menu == 'register' %}active{% endif %}"#}
{#                           href="{{ path('app_register') }}">S'inscrire</a>#}
                    {% endif %}
                </ul>
            </div>
        </nav>
    {#
        <div class="nav">
            <div class="nav-logo">
                <a href="{{ path("index") }}"><img src="{{ asset("img/logo.svg") }}" alt="Agence Pop Web logo"/></a>
            </div>
            <div class="nav-menu">
                <div class="nav-links">
                    <a class="nav-link {% if current_menu is defined and current_menu == 'about' %}active{% endif %}"
                       href="{{ path('about') }}">Agence</a>
                    <a class="nav-link {% if current_menu is defined and current_menu == 'service' %}active{% endif %}"
                       href="{{ path('service.index') }}">Services <span class="sr-only">(current)</span></a>
                    <a class="nav-link {% if current_menu is defined and current_menu == 'projects' %}active{% endif %}"
                       href="{{ path('project.index') }}">Réalisations <span class="sr-only">(current)</span></a>
                    <a class="nav-link {% if current_menu is defined and current_menu == 'articles' %}active{% endif %}"
                       href="{{ path('articles.index') }}">Blog <span class="sr-only">(current)</span></a>
                    <a class="nav-link {% if current_menu is defined and current_menu == 'jobs' %}active{% endif %}"
                       href="{{ path('job.index') }}">Recrutement</a>
                    <a class="nav-link {% if current_menu is defined and current_menu == 'contact' %}active{% endif %}"
                       href="{{ path('contact.show') }}">Contactez-nous</a>
                    {% if app.user %}



                        <a class="nav-link " href="/admin">Admin</a>

                        <a class=" {% if current_menu is defined and current_menu == 'logout' %}active{% endif %}"
                           href="{{ path('app_logout') }}">Se déconnecter</a>

                    {% else %}
                        #}{#
                       <a class=" {% if current_menu is defined and current_menu == 'login' %}active{% endif %}" href="{{ path('app_login') }}">Se connecter</a>
                       <a class=" {% if current_menu is defined and current_menu == 'register' %}active{% endif %}" href="{{ path('app_register') }}">S'inscrire</a>
                           #}{#
                    {% endif %}
                </div>#}

    {#<div class="nav-responsive">
        <div id="svg">
            <svg class="ham hamRotate hammove" viewBox="0 0 100 100" width="80" onclick="this.classList.toggle
    ('active')">
                <path class="line top"
                      d="m 30,33 h 40 c 3.722839,0 7.5,3.126468 7.5,8.578427 0,5.451959 -2.727029,8.421573 -7.5,8.421573 h -20"/>
                <path class="line middle" d="m 30,50 h 40"/>
                <path class="line bottom"
                      d="m 70,67 h -40 c 0,0 -7.5,-0.802118 -7.5,-8.365747 0,-7.563629 7.5,-8.634253 7.5,-8.634253 h 20"/>
            </svg>
        </div>
        <div id="nav-links-responsive">
            <a href="#">Test</a>
            <a href="#">Test</a>
            <a href="#">Test</a>
            <a href="#">Test</a>
        </div>
    </div>#}
    </div>
    </div>

</header>

<script>
    $(document).ready(function () {

        $('.nav-responsive').hide();
        /* $('#svg').hide();
         $('#nav-links-responsive').hide();
 */
        /* if (window.matchMedia("(max-width: 900px)").matches) {
             $('#svg').show();
         } else {
             $('#svg').hide();
         }*/

        /* $('.nav-responsive').click(function () {
             $('#nav-links-responsive').slideToggle();
         });*/
    });
</script>
