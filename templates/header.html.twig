

<header>
    <div id="marge-du-header">
        <a href="{{ path("index") }}"><img src="{{ asset("img/logo.svg") }}" alt="Agence Pop Web logo"/></a>
        <div id="menu-du-header">

            <a class="nav-link {% if current_menu is defined and current_menu == 'about' %}active{% endif %}" href="{{path('about')}}">Agence</a>
            <a class="nav-link {% if current_menu is defined and current_menu == 'service' %}active{% endif %}" href="{{path('service.index')}}">Services <span class="sr-only">(current)</span></a>
            <a class="nav-link {% if current_menu is defined and current_menu == 'index' %}active{% endif %}" href="{{path('index')}}#troisieme-segment">Réalisations <span class="sr-only">(current)</span></a>
            <a class="nav-link {% if current_menu is defined and current_menu == 'articles' %}active{% endif %}" href="{{path('articles.index')}}">Blog <span class="sr-only">(current)</span></a>
            <a class="nav-link {% if current_menu is defined and current_menu == 'jobs' %}active{% endif %}" href="{{path('job.index')}}">Recrutement</a>
            <a class="nav-link {% if current_menu is defined and current_menu == 'index' %}active{% endif %}" href="{{path('index')}}#quatrieme-segment">Contactez-nous</a>
            {% if app.user %}

                <a class=" {% if current_menu is defined and current_menu == 'logout' %}active{% endif %}" href="{{ path('app_logout') }}">Se déconnecter</a>

                {% if app.environment == "dev" %}
                <a class=" " href="admin">Admin</a>
                {% endif %}

                {% if app.environment == "prod" %}
                    <a class=" " href="/popweb/admin">Admin</a>
                {% endif %}

            {% else %}
                {#
               <a class=" {% if current_menu is defined and current_menu == 'login' %}active{% endif %}" href="{{ path('app_login') }}">Se connecter</a>
               <a class=" {% if current_menu is defined and current_menu == 'register' %}active{% endif %}" href="{{ path('app_register') }}">S'inscrire</a>
                   #}
            {%  endif  %}


        </div>
    </div>
</header>
<picture style="width: 100%">
    <source media="(min-width: 1200px)" srcset="{{ asset('pho/lens-l.jpg') }}">
    <source media="(min-width: 0px)" srcset="{{ asset('pho/lens-l.jpg') }}">
    <img src="{{ asset('pho/lens-l.jpg') }}" alt="Vue de Lens">
</picture>
