{% extends 'base.html.twig' %}

{% block title %}
    {{ job.title }}
{% endblock %}

{% block body %}
<div class="img-banner">
<img src="{{ asset('pho/banner-recrutement.png') }}" alt="Bannière recrutement" class="banner">
<div class="text">
    <h1>Titre</h2>
    <p>petit texte</p>
    </div>
</div>

    {% for message in app.flashes('success') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
    {% endfor %}

    <div class="container">
        <div class="row">
            <div class="col-md-12 article-item-show">
                <div id="scene">
                    <img  class="d-block w-100 lazyload p-img"
                         src="{{ asset("img/low_res.jpg") }}"
                         data-src="{{ app_path_jobs_images }}{{ job.image }}"
                         alt="AlternateText"
                         height="290px"/>
                    <img src="{{ app_path_default_images }}bg3.gif" class="animated"  id="anim" alt="">
                    <h1 class="p-title">{{ job.title }}</h1>
                </div>

                <div class="p-text">


                    <p class="p-description">{{ job.text | raw }}</p>
                    <p class="p-date">{{ job.updatedAt | date('d/m/Y H:i', timezone='Europe/Paris') }} </p>

                </div>
            </div>
        </div>


    </div>


{% endblock %}
{% block javascripts %}

    <script>

        $(document).ready(()=>{
            $(function () {
                $(window).scroll(function () {
                    var scroll = $(window).scrollTop();
                    if (scroll >= 300) {
                        $(".animated")[0].classList.add('active');

                    } else {

                    }
                });
            });
        });


    </script>
{% endblock %}



